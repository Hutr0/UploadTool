#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")"

bash ./test.sh "$@"

# Чтобы не зависать в CI/скриптах — ждём ввода только в интерактивном терминале.
if [[ -t 0 ]]; then
  echo
  read -n 1 -s -r -p "Нажми любую клавишу, чтобы закрыть..."
  echo
fi
