#!/bin/bash
set -euo pipefail

cd "$(dirname "$0")"

bash ./test.sh "$@"

# To avoid hanging in CI/scripts — wait for input only in an interactive terminal.
if [[ -t 0 ]]; then
  echo
  read -n 1 -s -r -p "Press any key to close..."
  echo
fi
